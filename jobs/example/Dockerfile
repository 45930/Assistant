FROM chief-of-staff-job-base:latest

# Add job-specific system dependencies here.
# The base image already includes: node 22, claude-code, gws CLI, tsx, curl, jq.
#
# Examples:
#   RUN apt-get update && apt-get install -y python3-pip && rm -rf /var/lib/apt/lists/*
#   RUN npm install -g some-package
#   COPY my-script.py /workspace/scripts/
